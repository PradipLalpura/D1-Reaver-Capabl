# Shot list + edit directions (screen sections: record at 1280×800, localhost)

## SHOTS (in order)
1. Playground load → loader → Armory (5s, establish).
2. Paste keys → Arm keys → target typed → Strike (real time, trim dead air only).
3. Steps streaming (keep 3 wave moments; speed-ramp the middle 2×).
4. Lead cards + why-expander open on one QUALIFIED lead (pause 3s).
5. Conflict card / UNCERTAIN lead with reasons (pause 3s).
6. CSV download → open in editor, scroll verdict_summary column.
7. `/mcp-docs` scroll (tabs + matrix) → `/connect` token widget (blur the actual token).
8. Claude Code terminal: `setup_mcp.py --check` → 10 live tools (credibility shot).

## EDIT RULES
- Cuts on step completions (compile:ok, discover:N, export:csv) — the engine sets the rhythm.
- Grade: crush blacks toward #070c16, push golds; match Flow scenes' palette.
- Music: tension → resolve at Scene 2 bolt; drop to minimal pulse under terminal.
- Never stage a result: if a live run fails on camera, show the honest error card
  (it's on-brand) or cut to the golden run — never composite fake leads.
- Captions: burn in styled per FLOW_PROMPTS.md; keep .srt as the timed master.
